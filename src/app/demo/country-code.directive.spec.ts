import { CountryCodeDirective } from './country-code.directive';

describe('CountryCodeDirective', () => {
  it('should create an instance', () => {
    const directive = new CountryCodeDirective();
    expect(directive).toBeTruthy();
  });
});
